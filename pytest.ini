[pytest]
markers =
    smoke: marker for smoke tests
    regression: marker for regression tests
    register: marker for registration tests
    login: marker for user login test from product page
    login_guest: marker for login test as guest
